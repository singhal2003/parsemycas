// UNUSED — this was for the old server-redirect Google OAuth flow. Google sign-in
// is now client-side (Google Identity Services via @react-oauth/google, see
// pages/Login.jsx), so there's no redirect callback step anymore. Safe to delete
// this file.
